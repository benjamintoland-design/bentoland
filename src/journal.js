const jjson=(data,init={})=>Response.json(data,{...init,headers:{'cache-control':'no-store',...(init.headers||{})}});
function cookieValue(r,n){for(const p of (r.headers.get('cookie')||'').split(';')){const[k,...v]=p.trim().split('=');if(k===n)return v.join('=')}return''}
async function sessionToken(e){if(!e.ADMIN_KEY)return'';const enc=new TextEncoder();const k=await crypto.subtle.importKey('raw',enc.encode(e.ADMIN_KEY.trim()),{name:'HMAC',hash:'SHA-256'},false,['sign']);const sig=new Uint8Array(await crypto.subtle.sign('HMAC',k,enc.encode('bentoland-admin-session-v1')));let b='';for(const x of sig)b+=String.fromCharCode(x);return btoa(b).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/g,'')}
async function adminAuthorized(r,e){if(typeof e.ADMIN_KEY!=='string'||!e.ADMIN_KEY.length)return false;const c=cookieValue(r,'bt_admin');return c?c===await sessionToken(e):false}
function safeName(n='image'){return String(n).toLowerCase().replace(/[^a-z0-9._-]+/g,'-').replace(/^-+|-+$/g,'')||'image'}
function slugify(v='entry'){return String(v).toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'')||`entry-${Date.now()}`}
function vis(v){return ['draft','private','public'].includes(v)?v:'draft'}
async function ensureJournalSchema(db){await db.prepare(`CREATE TABLE IF NOT EXISTS journal_entries (id INTEGER PRIMARY KEY AUTOINCREMENT,slug TEXT NOT NULL UNIQUE,title TEXT NOT NULL,subtitle TEXT NOT NULL DEFAULT '',body TEXT NOT NULL DEFAULT '',location TEXT NOT NULL DEFAULT '',entry_date TEXT NOT NULL DEFAULT '',camera TEXT NOT NULL DEFAULT '',lens TEXT NOT NULL DEFAULT '',visibility TEXT NOT NULL DEFAULT 'draft',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();await db.prepare(`CREATE TABLE IF NOT EXISTS journal_images (id INTEGER PRIMARY KEY AUTOINCREMENT,entry_id INTEGER NOT NULL,r2_key TEXT NOT NULL,filename TEXT NOT NULL DEFAULT '',caption TEXT NOT NULL DEFAULT '',sort_order INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();await db.prepare('CREATE INDEX IF NOT EXISTS idx_journal_entries_visibility ON journal_entries(visibility,entry_date,created_at)').run();await db.prepare('CREATE INDEX IF NOT EXISTS idx_journal_images_entry ON journal_images(entry_id,sort_order,id)').run()}
async function hydrate(db,row,allowPrivate){const{results}=await db.prepare('SELECT id,filename,caption,sort_order FROM journal_images WHERE entry_id=? ORDER BY sort_order ASC,id ASC').bind(row.id).all();const images=results.map(x=>({...x,url:`/journal-media/${x.id}`}));return{...row,images,cover:images[0]?.url||null,can_view_private:allowPrivate}}
async function storeImages(e,entryId,slug,files){let order=0;for(const file of files){if(!(file instanceof File)||!file.size||!file.type.startsWith('image/'))continue;const key=`journal/${slug}/${Date.now()}-${crypto.randomUUID().slice(0,8)}-${safeName(file.name)}`;await e.PHOTOS.put(key,file.stream(),{httpMetadata:{contentType:file.type}});await e.DB.prepare('INSERT INTO journal_images (entry_id,r2_key,filename,sort_order) VALUES (?,?,?,?)').bind(entryId,key,file.name,order++).run()}}
function assetRequest(r,path){const u=new URL(r.url);u.pathname=path;u.search='';return new Request(u.toString(),{method:'GET',headers:r.headers})}
export async function handleJournalRequest(r,e){const u=new URL(r.url),p=u.pathname;const relevant=p==='/journal'||p==='/admin/journal'||p.startsWith('/journal-api/')||p.startsWith('/journal-media/');if(!relevant)return null;if(p==='/journal')return e.ASSETS.fetch(assetRequest(r,'/journal.html'));if(p==='/admin/journal'){if(!await adminAuthorized(r,e))return new Response(null,{status:303,headers:{location:'/admin'}});return e.ASSETS.fetch(assetRequest(r,'/admin-journal.html'))}try{await ensureJournalSchema(e.DB)}catch(err){return jjson({error:'Journal database unavailable.',detail:String(err?.message||err)},{status:500})}
if(p==='/journal-api/entries'&&r.method==='GET'){const admin=await adminAuthorized(r,e);const wantPrivate=u.searchParams.get('all')==='1'&&admin;const q=wantPrivate?e.DB.prepare("SELECT * FROM journal_entries ORDER BY COALESCE(NULLIF(entry_date,''),created_at) DESC,created_at DESC"):e.DB.prepare("SELECT * FROM journal_entries WHERE visibility='public' ORDER BY COALESCE(NULLIF(entry_date,''),created_at) DESC,created_at DESC");const{results}=await q.all();return jjson({entries:await Promise.all(results.map(x=>hydrate(e.DB,x,wantPrivate)))})}
const entryMatch=p.match(/^\/journal-api\/entries\/([^/]+)$/);if(entryMatch&&r.method==='GET'){const key=decodeURIComponent(entryMatch[1]);const row=/^\d+$/.test(key)?await e.DB.prepare('SELECT * FROM journal_entries WHERE id=?').bind(Number(key)).first():await e.DB.prepare('SELECT * FROM journal_entries WHERE slug=?').bind(key).first();if(!row)return jjson({error:'Entry not found.'},{status:404});const admin=await adminAuthorized(r,e);if(row.visibility!=='public'&&!admin)return jjson({error:'Entry not found.'},{status:404});return jjson({entry:await hydrate(e.DB,row,admin)})}
if(p==='/journal-api/admin/entries'&&r.method==='POST'){if(!await adminAuthorized(r,e))return jjson({error:'Unauthorized.'},{status:403});const f=await r.formData();const title=String(f.get('title')||'').trim();if(!title)return jjson({error:'Title is required.'},{status:400});const slug=`${slugify(title)}-${crypto.randomUUID().slice(0,8)}`;const result=await e.DB.prepare('INSERT INTO journal_entries (slug,title,subtitle,body,location,entry_date,camera,lens,visibility) VALUES (?,?,?,?,?,?,?,?,?)').bind(slug,title,String(f.get('subtitle')||''),String(f.get('body')||''),String(f.get('location')||''),String(f.get('entry_date')||''),String(f.get('camera')||''),String(f.get('lens')||''),vis(String(f.get('visibility')||'draft'))).run();const id=Number(result.meta.last_row_id);await storeImages(e,id,slug,f.getAll('images'));return jjson({ok:true,id,slug})}
const adminEntry=p.match(/^\/journal-api\/admin\/entries\/(\d+)$/);if(adminEntry&&r.method==='PATCH'){if(!await adminAuthorized(r,e))return jjson({error:'Unauthorized.'},{status:403});const id=Number(adminEntry[1]);const cur=await e.DB.prepare('SELECT * FROM journal_entries WHERE id=?').bind(id).first();if(!cur)return jjson({error:'Entry not found.'},{status:404});const b=await r.json().catch(()=>({}));const v={title:Object.hasOwn(b,'title')?String(b.title||'').trim():cur.title,subtitle:Object.hasOwn(b,'subtitle')?String(b.subtitle||''):cur.subtitle,body:Object.hasOwn(b,'body')?String(b.body||''):cur.body,location:Object.hasOwn(b,'location')?String(b.location||''):cur.location,entry_date:Object.hasOwn(b,'entry_date')?String(b.entry_date||''):cur.entry_date,camera:Object.hasOwn(b,'camera')?String(b.camera||''):cur.camera,lens:Object.hasOwn(b,'lens')?String(b.lens||''):cur.lens,visibility:Object.hasOwn(b,'visibility')?vis(String(b.visibility)):cur.visibility};if(!v.title)return jjson({error:'Title is required.'},{status:400});await e.DB.prepare('UPDATE journal_entries SET title=?,subtitle=?,body=?,location=?,entry_date=?,camera=?,lens=?,visibility=?,updated_at=CURRENT_TIMESTAMP WHERE id=?').bind(v.title,v.subtitle,v.body,v.location,v.entry_date,v.camera,v.lens,v.visibility,id).run();return jjson({ok:true})}
if(adminEntry&&r.method==='DELETE'){if(!await adminAuthorized(r,e))return jjson({error:'Unauthorized.'},{status:403});const id=Number(adminEntry[1]);const{results}=await e.DB.prepare('SELECT r2_key FROM journal_images WHERE entry_id=?').bind(id).all();for(const x of results)try{await e.PHOTOS.delete(x.r2_key)}catch{}await e.DB.prepare('DELETE FROM journal_images WHERE entry_id=?').bind(id).run();await e.DB.prepare('DELETE FROM journal_entries WHERE id=?').bind(id).run();return jjson({ok:true})}
const uploadMatch=p.match(/^\/journal-api\/admin\/entries\/(\d+)\/images$/);if(uploadMatch&&r.method==='POST'){if(!await adminAuthorized(r,e))return jjson({error:'Unauthorized.'},{status:403});const id=Number(uploadMatch[1]);const row=await e.DB.prepare('SELECT slug FROM journal_entries WHERE id=?').bind(id).first();if(!row)return jjson({error:'Entry not found.'},{status:404});const f=await r.formData();await storeImages(e,id,row.slug,f.getAll('images'));await e.DB.prepare('UPDATE journal_entries SET updated_at=CURRENT_TIMESTAMP WHERE id=?').bind(id).run();return jjson({ok:true})}
const imageDelete=p.match(/^\/journal-api\/admin\/images\/(\d+)$/);if(imageDelete&&r.method==='DELETE'){if(!await adminAuthorized(r,e))return jjson({error:'Unauthorized.'},{status:403});const id=Number(imageDelete[1]);const row=await e.DB.prepare('SELECT r2_key FROM journal_images WHERE id=?').bind(id).first();if(!row)return jjson({error:'Image not found.'},{status:404});try{await e.PHOTOS.delete(row.r2_key)}catch{}await e.DB.prepare('DELETE FROM journal_images WHERE id=?').bind(id).run();return jjson({ok:true})}
const media=p.match(/^\/journal-media\/(\d+)$/);if(media&&r.method==='GET'){const id=Number(media[1]);const row=await e.DB.prepare('SELECT i.r2_key,e.visibility FROM journal_images i JOIN journal_entries e ON e.id=i.entry_id WHERE i.id=?').bind(id).first();if(!row)return new Response('Not found',{status:404});const admin=row.visibility!=='public'?await adminAuthorized(r,e):false;if(row.visibility!=='public'&&!admin)return new Response('Not found',{status:404});const o=await e.PHOTOS.get(row.r2_key);if(!o)return new Response('Not found',{status:404});const h=new Headers();o.writeHttpMetadata(h);h.set('etag',o.httpEtag);h.set('cache-control',row.visibility==='public'?'public,max-age=86400':'private,no-store');return new Response(o.body,{headers:h})}return jjson({error:'Journal route not found.'},{status:404})}
